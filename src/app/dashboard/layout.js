export const metadata = {
  title: 'Dashboard - BookFlix',
  description: 'Your personal library dashboard - manage your books, reservations, and account',
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}

